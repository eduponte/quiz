<form method="get" action="/quizes/<%= quiz.id %>/answer">
  Pregunta: <%= quiz.pregunta %>
  <input type="text" name="respuesta" value="Responda aquí"/>
  <input type="submit" value="Enviar"/>
</form>

<p><a href="/quizes/<%= quiz.id %>/comments/new"><button>Nuevo comentario</button></a></p>
<%for(var i in quiz.Comments){ comment = quiz.Comments[i];%>
  <%if(comment.publicado || session.user){%>
    <p>- <%=comment.texto%>
    <%if(session.user && !comment.publicado){%>
      <a href="/quizes/<%=quiz.id%>/comments/<%=comment.id%>/publish"><button>Publicar</button></a>
    <%}%>
    </p>
  <%}%>
<%}%>
